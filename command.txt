1. Команды консоли (основные)
+++++++++++++++++++++++++++++++++++++++++++++++++++++
pwd 	текущий каталог
mkdir	создание директории // -p {dir1,dir2} несколько диреторий
cd !$	перейти в только что созданную
   ../.. на 2 уровня выше
   -	предидущая // ` домашняя
cp [file1] [file2]	копирование
mv [file1] [file2]	перемещение
   mv [dir1]/*.* [dir2]	перемещение всех файлов
rm [file-name]	удаление
rmdir [dir-name]	удаление пустой директории // -r непустой(rm -rf [dir-name])
find -iname "*1.txt"
   подробно: https://habr.com/ru/company/alexhost/blog/525394/

полезное https://habr.com/ru/post/522078/

2. Команды Git
+++++++++++++++++++++++++++++++++++++++++++++++++++++
git config –global user.name "имя гита"
git config –global user.email "…."
git clone https….	клонировать с гита по ссылке (создает каталог с именем клонируемого репозитария и папкой ".git" внутри)
git branch 	показать ветки // name создать ветку
git checkout	перейти в ветку // -b 	и перейти
git merge 	вмержить в текущую ветку :)
git status	статус
git add .	добавить все файлы в папке (состояние в активной ветке)
git add names.json	добавить файл "names.json"
git init	Инициализация/создание нового локального репозитория в текущей папке
git commit -m "text commit"
git commit –am "…"	добавляет (add .) и коммитит. только для файлов modified:
git push	jnghfdrf
git fetch	проверка на наличие изменений во внешнем, которых нет в локальном
git pull	стягиваем с внешнего репозитария

3. grep !!!
$ grep [опции] шаблон [имя файла...]
Или:
$ команда | grep [опции] шаблон
	-b - показывать номер блока перед строкой;
	-c - подсчитать количество вхождений шаблона;
	-h - не выводить имя файла в результатах поиска внутри файлов Linux;
	-i - не учитывать регистр;
	- l - отобразить только имена файлов, в которых найден шаблон;
	-n - показывать номер строки в файле;
	-s - не показывать сообщения об ошибках;
	-v - инвертировать поиск, выдавать все строки кроме тех, что содержат шаблон;
	-w - искать шаблон как слово, окружённое пробелами;
	-e - использовать регулярные выражения при поиске;
	-An - показать вхождение и n строк до него;
	-Bn - показать вхождение и n строк после него;
	-Cn - показать n строк до и после вхождения;





